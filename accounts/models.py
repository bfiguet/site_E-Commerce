from django.db import models
from django.contrib.auth.models import AbstractUser

# Create your models here.
class	Shopper(AbstractUser):
	pass
	#code de la view add_to_cart() peut-etre fait ici pour alleger la view